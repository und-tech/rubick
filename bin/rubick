#!/usr/bin/env python3

if __name__ == '__main__':
    from rubick_pkg.rubick import cli, logger

    try:
        cli()
    except Exception as e:
        log = logger.create('rubick_bin')
        log.error(e)
